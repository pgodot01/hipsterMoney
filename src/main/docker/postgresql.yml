version: '2'
services:
    realestate-postgresql:
        image: postgres:10.4
        # volumes:
        #     - ~/volumes/jhipster/realestate/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=realestate
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
